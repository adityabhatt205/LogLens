services:
  loglens:
    build: .
    image: loglens:latest
    container_name: loglens
    restart: unless-stopped
    ports:
      - "${LOGLENS_PORT:-8080}:8080"
    volumes:
      # Persistent SQLite database
      - loglens-data:/data
      # Optional: mount your config.yaml (copy config.yaml.example first)
      # - ./config.yaml:/app/config.yaml:ro
    environment:
      # Bearer token for REST API (leave empty to disable auth)
      - LOGLENS_API_TOKEN=${LOGLENS_API_TOKEN:-}
      # PII salt for pseudonymization (set a random secret)
      - LOGLENS_PII_SALT=${LOGLENS_PII_SALT:-}
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8080/api/v1/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  loglens-data:
    driver: local
